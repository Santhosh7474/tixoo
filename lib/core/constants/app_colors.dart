// lib/core/constants/app_colors.dart

import 'package:flutter/material.dart';

class AppColors {
  // Primary Green (Used for "Get Plus", "Book Now", "Date Text" etc.)
  static const Color primaryGreen = Color(0xFF388E3C); 
  
  // Secondary Green (Darker shade, used for the large bottom card on HomeScreen)
  static const Color secondaryGreen = Color(0xFF2E7D32);
  
  // Light Green (Used in the "Assured" tag and sometimes for icons)
  static const Color lightGreen = Color(0xFF66BB6A); 

  // Dark Text/Primary Text (Used for large titles and primary text)
  static const Color darkText = Color(0xFF212121); 

  // Secondary Text/Location Text (Used for subtitles and secondary information)
  static const Color greyText = Color(0xFF757575); 

  // Background/Card Color (Off-white/light gray for card backgrounds)
  static const Color cardBackground = Color(0xFFFAFAFA); 

  // Pink Banner Color (Gradient colors for the Discovery Screen top section)
  static const Color pinkBannerTop = Color(0xFFFFEBEE); 
  static const Color pinkBannerBottom = Color(0xFFFAE0E4); 
  
  // Dark Category Button Color (Used for the Music/Standup tabs background)
  static const Color darkCategory = Color(0xFF263238); 
  
  // Main Scaffold Background
  static const Color background = Colors.white; 
}